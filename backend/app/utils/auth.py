# backend/app/utils/auth.py
from pyJWT import PyJWT as jwt

# Placeholder for any JWT or token-based logic to add later.

def verify_token(token: str) -> bool:
    jwt_instance = jwt()
    
    
    return True
